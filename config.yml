title: python_amazica
description: Let's learn together
template:
  repo: python_amazica-template
  name: python_amazica
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
